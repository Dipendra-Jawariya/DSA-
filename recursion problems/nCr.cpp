// Find nCr
// You are given two numbers n and r. You need to find nCr.
// nCr = (n!) / ((n-r)!*(r!))

#include <iostream>
using namespace std;
int nCr(int n, int r)
{
}
int main()
{
    int n;
    cin >> n;
    int r;
    cin >> r;
    nCr(n, r);
    return 0;
}